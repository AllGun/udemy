let contadorDecrescente = 5;

do {
  console.log(`Número atual é ${contadorDecrescente}`);
  contadorDecrescente--;
} while (contadorDecrescente > 0);

let contadorCrescente = 0;

do {
  console.log(`Número atual é ${contadorCrescente}`);
  contadorCrescente++;
} while (contadorCrescente < 10);
