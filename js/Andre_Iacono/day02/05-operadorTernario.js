let velocidade = 120;
let velocidadePermitida = velocidade > 110 ? "proibida" : "permitida";
console.log(`Sua velocidade é ${velocidadePermitida}`);

velocidade = 80;
velocidadePermitida = velocidade > 110 ? "proibida" : "permitida";
console.log(`Sua velocidade é ${velocidadePermitida}`);
